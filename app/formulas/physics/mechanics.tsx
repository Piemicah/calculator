import FormulaList from "@/components/FormulaList";
import { mechanics } from "@/util/data";
import React from "react";

const Mechanics = () => {
  return <FormulaList data={mechanics} title="Straight Line Motion" />;
};

export default Mechanics;
