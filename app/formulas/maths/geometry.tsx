import FormulaList from "@/components/FormulaList";
import { geometry } from "@/util/data";
import React from "react";

const Geometry = () => {
  return <FormulaList data={geometry} title="geometry" />;
};

export default Geometry;
